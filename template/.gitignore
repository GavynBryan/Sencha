# The game module build tree.
/build/

# Cook output: regenerated from authored content by the editor Cook / cook path.
/assets/.cooked/

/assets/asset_ids.json

# Per-user editor view state (world focus/open/visible), never content.
*.sworld.user.json
