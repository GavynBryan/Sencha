Content the engine's own tests load.

It carries no game components and no gameplay vocabulary, so a test game that
registers nothing can load it. That is the point: a fixture that needs a
particular game module to be loadable makes the test about that module.

`.cooked/` here is generated by the cook fixtures in `test/CMakeLists.txt` and
is not committed.
