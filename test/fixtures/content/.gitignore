# Cook output and the id map the cook maintains: generated by the cook fixtures
# in test/CMakeLists.txt and by the golden-image tests on every run, never
# committed. A committed cook cache rots against the current format and its
# content-hash dedup cross-links artifacts between roots.
.cooked/
asset_ids.json
