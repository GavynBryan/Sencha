# Cook output and the id map the cook maintains: generated by the fixtures in
# test/CMakeLists.txt on every run, never committed. A committed cook cache
# rots against the current format and its content-hash dedup cross-links
# artifacts between roots.
.cooked/
asset_ids.json
