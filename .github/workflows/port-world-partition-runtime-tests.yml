name: Port world partition runtime tests

on:
  push:
    paths:
      - '.github/workflows/port-world-partition-runtime-tests.yml'
      - 'tools/temporary/port_world_partition_runtime_tests.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out cutover branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - name: Apply world partition test cutover
        run: python3 tools/temporary/port_world_partition_runtime_tests.py
      - name: Commit clean source
        run: |
          git config user.name "sencha-unified-runtime"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "test(zone): port world partition policy to RuntimeWorld"
          git push origin HEAD:${GITHUB_REF_NAME}
